#ifndef DATAEDITOR_H
#define DATAEDITOR_H


#include <string>
#include <map>
#include <unordered_map>
#include <unordered_set>
#include <filesystem>

#include <nlohmannJson/json.hpp>
#include "models/application/UserOrientation.h"
#include "models/project/ProjectInfos.h"
#include "models/application/Author.h"

//new
/*
namespace DataEditor
{
	//void Edit(const Tag& input, Tag& output, const std::unordered_set<std::string> carac);
	void Edit(const Cluster& input,  Cluster& output, const std::unordered_set<std::string> carac);
	void Edit(const MasterCluster& input, MasterCluster& output, const std::unordered_set<std::string> carac);
	void Edit(const Scan& input,  Scan& output ,const std::unordered_set<std::string> carac);
	void Edit(const Box& input,  Box& output, const std::unordered_set<std::string> carac);
	void Edit(const Grid& input,  Grid& output, const std::unordered_set<std::string> carac);
	void Edit(const Cylinder& input,  Cylinder& output, const std::unordered_set<std::string> carac);
	void Edit(const Torus& input,  Torus& output, const std::unordered_set<std::string> carac);
	void Edit(const Piping& input,  Piping& output, const std::unordered_set<std::string> carac);
	void Edit(const Sphere& input, Sphere& output, const std::unordered_set<std::string> carac);
	void Edit(const Point& input,  Point& output, const std::unordered_set<std::string> carac);
	void Edit(const PCObject& input,  PCObject& output, const std::unordered_set<std::string> carac);
	void Edit(const MeshObject& input,  MeshObject& output, const std::unordered_set<std::string> carac);
	void Edit(const SimpleMeasure& input,  SimpleMeasure& output, const std::unordered_set<std::string> carac);
	void Edit(const PolylineMeasure& input,  PolylineMeasure& output, const std::unordered_set<std::string> carac);
	void Edit(const PointToPlaneMeasure& input,  PointToPlaneMeasure& output, const std::unordered_set<std::string> carac);
	void Edit(const PointToPipeMeasure& input,  PointToPipeMeasure& output, const std::unordered_set<std::string> carac);
	void Edit(const PipeToPlaneMeasure& input,  PipeToPlaneMeasure& output, const std::unordered_set<std::string> carac);
	void Edit(const PipeToPipeMeasure& input,  PipeToPipeMeasure& output, const std::unordered_set<std::string> carac);
	void Edit(const BeamBendingMeasure& input,  BeamBendingMeasure& output, const std::unordered_set<std::string> carac);
	void Edit(const ColumnTiltMeasure& input,  ColumnTiltMeasure& output, const std::unordered_set<std::string> carac);
	void Edit(const UserOrientation& input,  UserOrientation& output, const std::unordered_set<std::string> carac);
	void Edit(const ViewPoint& input,  ViewPoint& output, const std::unordered_set<std::string> carac);
	void Edit(const ProjectInfos& input,  ProjectInfos& output, const std::unordered_set<std::string> carac);
	//void Edit(const sma::TagTemplate& input,  sma::TagTemplate& output, const std::unordered_set<std::string> carac);

	void EditGenericProperties(const Data& input, Data& output, const std::unordered_set<std::string> carac);
	void EditClippingProperties(const Clipping& input,  Clipping& output, const std::unordered_set<std::string> carac);
	void EditObject3DProperties(const Object3D& input,  Object3D& output, const std::unordered_set<std::string> carac);

}*/
#endif // !DATAEDITOR_H_ 
