#ifndef GUI_RENDERING_TYPES_H
#define GUI_RENDERING_TYPES_H

struct RampScale
{
    bool showScale;
    bool centerBoxScale;
    int graduationCount;
    bool showTemperatureScale;
    // enum colorScheme ??
};

#endif
